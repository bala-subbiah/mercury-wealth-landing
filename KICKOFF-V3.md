# Kickoff prompt — v3 build session

Fresh Claude Code session in this folder, Fable, Auto mode. Paste:

---

We are building v3 as a COMPLETELY NEW site — not an edit of v2. The team rejected
v2's register (too much text, product-explanation feel); the full story is in the
docs. First preserve v2 on a git branch/tag, then build fresh per the brief, using
v2's copy only as the audited fact source to tighten.

Read, in order: `CLAUDE.md` · `docs/team-feedback.md` (the team's consolidated
feedback — the ground truth) · `docs/v3-brief.md` (your brief: direction and hard
constraints only; the design craft is yours).

Then, before writing any code: open and deeply study the three reference sites
(v7labs.com, capsa.ai, rogo.com) in the browser — every page, noting nav, headers,
section composition, content density, footers, graphics, and animation. They are the
primary design source for every section of this build (the brief's most important
instruction). Also study the Gemini mockup at
`docs/gemini-build-mockup/mercury-digital-remix-test/` — the team liked its HK hero
video, short crisp hero text, and logo marquee; treat those as taste signals to build
on, not a fixed spec. Then rebuild the site to the brief. Iterate on the dev server until it is right on
BOTH desktop and mobile — check mobile at every step, not at the end.

Pause only twice: show me the new homepage when it first feels done, and confirm with
me before any public deploy. Otherwise keep going without asking. If something needs
an owner decision that the brief doesn't cover, ask rather than invent — especially
anything touching claims, names, or relationships.

---
