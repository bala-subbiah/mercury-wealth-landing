# Canonical public base URL for the marketing site.
# Used via Vite's native %VITE_SITE_URL% HTML replacement in every entry's
# <head> (canonical, og:url, og:image, twitter:image). Keep public/sitemap.xml
# and public/robots.txt in sync manually — they are static files and are not
# processed by Vite's env replacement.
VITE_SITE_URL=https://mercury-wealth-landing.vercel.app
