/* Entry for /legal/terms/ — paired with legal/terms/index.html. */
import "../../boot.ts";
import { mount } from "../../chrome/mount.tsx";
import TermsPage from "./TermsPage.tsx";

mount(<TermsPage />);
